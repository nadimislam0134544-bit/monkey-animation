setInterval(() => {

    const slide = document.querySelector(".slide");

    slide.style.animation = "none";
    slide.offsetHeight;

    slide.style.animation = "slideDown 2s ease forwards";

},3000);
